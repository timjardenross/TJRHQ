python-telegram-bot==20.7
apscheduler==3.11.3
# USS-TJR-MSN-0370: bumped 2.3.4 -> 2.7.4, NOT the full Dependabot PR #157
# target (2.31.0) — see scoped_supabase.py's module docstring and
# knowledge/missions/USS-TJR-MSN-0370-supabase-investigation-knowledge-record.md.
# 2.7.4 is the highest supabase-py release whose OWN declared dependency
# bounds (postgrest<0.17.0) still keep postgrest below the version where it
# started passing httpx's `proxy=` kwarg (added in httpx 0.26, needs
# `gotrue<2.9.0` too, pinned below for the same reason) — required because
# python-telegram-bot 20.7 hard-pins httpx~=0.25.2. supabase>=2.8.1 forces
# postgrest>=0.17.0, which crashes under httpx<0.26 with
# "Client.__init__() got an unexpected keyword argument 'proxy'"; verified
# via a real ResolutionImpossible + live TypeError reproduction, not just
# reading changelogs. Re-verify from scratch (see knowledge record) before
# ever bumping past 2.7.4 while python-telegram-bot 20.7's httpx pin stands.
supabase==2.7.4
# Must stay <2.9.0 — see the supabase pin comment above; gotrue>=2.9.0 also
# calls httpx.Client(proxy=...), which httpx<0.26 rejects. The previous
# gotrue==2.12.4 pin (from Dependabot PR #132) was ALREADY broken: it
# conflicts with `httpx<0.26` (gotrue 2.12.4 requires httpx>=0.26) and pip
# confirms ResolutionImpossible for that combination — the live xo-bot venv
# was silently still running the pre-#132 gotrue==1.3.1 the whole time
# because `pip install -r requirements.txt` was never actually re-run
# against this file after PR #132 merged. Fixed here as part of MSN-0370.
gotrue<2.9.0
python-dotenv==1.2.3
# httpx<0.26 required: python-telegram-bot 20.7 passes `proxies` kwarg removed in httpx 0.26+
httpx<0.26
pyjwt>=2.13.0
# edge-tts: Microsoft Edge neural TTS for XO voice replies — no API key required
edge-tts>=7.2.8
