# Telegram Bot Environment — copy to each bot's subdirectory as .env
# Never commit .env files with real tokens

TELEGRAM_BOT_TOKEN=        # From BotFather — /mybots → API Token
TELEGRAM_CHAT_ID=          # Captain's Telegram user ID — message @userinfobot to find it
SUPABASE_URL=https://cjvrpjwewsrumnbdydgg.supabase.co
SUPABASE_KEY=              # Supabase anon key (safe for read; use service-role for writes)
BOT_NAME=                  # XO | ChiefEngineer | EngineeringDept
