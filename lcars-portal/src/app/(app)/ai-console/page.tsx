// Retired — AI Console consolidated into Executive Staff (Phase 1, MSN-PORT-003).
import { redirect } from 'next/navigation';
export default function AIConsolePage() {
  redirect('/executive-staff');
}
