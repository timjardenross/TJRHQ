// Retired — Advisory Console consolidated into Executive Staff (Phase 1, MSN-PORT-003).
import { redirect } from 'next/navigation';
export default function AdvisoryPage() {
  redirect('/executive-staff');
}
