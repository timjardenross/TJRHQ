// Retired — Executive Staff consolidated into Chief of Staff (Phase 1, MSN-PORT-003).
import { redirect } from 'next/navigation';
export default function ExecutiveStaffPage() {
  redirect('/chief-of-staff');
}
