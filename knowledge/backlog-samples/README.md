# Backlog sample set — USS-TJR-MSN-0366 Stream 3

Small, real sample set used to exercise `tools/supabase/docling_ingest.py`
end-to-end. Not a slice of the actual 814-document review backlog
(`knowledge/OSS-Gap-Solutions-2026-08-23.md` GAP 5) — that backlog is
described there via `knowledge_utilisation.py`'s disk scan of this repo's
own markdown knowledge base, not a folder of PDFs/DOCX files reachable
from this sandbox. See the mission's knowledge record
(`knowledge/missions/DOCLING-KNOWLEDGE-INGESTION-20260912-knowledge-record.md`)
for the full accounting of what "backlog" actually refers to and why a
constructed sample set was used instead.

Files:
- `comms-backup-procedure.html` — constructed for this mission (headings,
  ordered/unordered lists, one table).

Two more real samples used for the same test run already lived at the
repo root (unrelated to this mission, from an earlier, unrelated PR) and
were used in place rather than duplicated here:
- `jarvis-setup-guide.pdf` (9-page real PDF, digital text)
- `command centre audit.pdf` (8-page real PDF, digital text)
- `emergency_alert_hub_mission_product_scope.docx` (real DOCX with 4 tables)
