# REVS Content Creation Agents - Python Dependencies
# Patched for Python 3.12 (VM default) and platform infra reuse:
#  - ollama/langchain dropped: parser calls the existing Local Model Router
#    (localhost:8891) over HTTP instead of a local Ollama client/agent framework.
#  - fastapi/uvicorn dropped: entry points are CLI + Telegram trigger, no API server planned.
#  - Stable Diffusion/Coqui TTS deps dropped: no GPU on this VM, hero art and podcast
#    narration route through the Gemini API instead (google-genai).
#  - markdown/svgwrite/Shapely/imageio/imageio-ffmpeg/moviepy/pydub/ffmpeg-python/click/tqdm
#    dropped: none are imported anywhere in src/ - video assembly shells out to the
#    ffmpeg CLI directly (subprocess), images go through PIL, not these.
#  - numpy/mypy bumped: pinned versions predate Python 3.12 support.

# Core
python-dotenv==1.2.3

# LLM (Local Model Router client)
pydantic==2.13.5
requests==2.34.2

# Image + speech generation (Gemini API - see gemini-imagegen skill)
google-genai==2.24.0
tenacity==9.1.4

# Text Processing
pypandoc==1.17
Weasyprint==70.0
PyPDF2==3.0.1

# Image & Graphics
Pillow==12.3.0
numpy==2.5.3

# Presentations
python-pptx==1.0.2

# Development
pytest==9.1.1
pytest-cov==7.1.0
black==26.5.1
pylint==4.0.8
mypy==2.3.1
flake8==7.3.0

# Utilities
pyyaml==6.0.3
