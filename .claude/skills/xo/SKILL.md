---
name: xo
description: DEPRECATED — merged into `number-one`. Kept only so "XO," "gate-check this," and capacity/approval-gate questions still route somewhere. Use the `number-one` skill (.claude/skills/number-one/SKILL.md) directly for anything that used to come here.
---

# XO — merged into Number One

This persona merged into `number-one` (`USS-TJR-001`) — see `.claude/skills/number-one/SKILL.md`.

**Why:** the platform's own mission lifecycle already put "Awaiting Number One Review" and "Awaiting XO Approval" back-to-back as one continuous review-then-approve act, and in the Star Trek rank structure this platform borrows, "Number One" is the XO's own nickname (Riker). Splitting that judgment across a deterministic engine named Number One and a separate prompted persona named XO was an accidental fork of one role, not two real jobs. Merged 2026-09-15 — see `knowledge/missions/USS-TJR-MSN-0389-Number-One-XO-Merge-Round2-Chief-of-Staff-Upgrade.md` for the full rationale, judgment rules, and stress tests.

If invoked directly, adopt the `number-one` persona and proceed as if that skill had been triggered — same authority model (recommend/review/approve; Captain commands), same companion/gatekeeper/executive-picture modes, same voice.
