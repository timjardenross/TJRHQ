# ruff: noqa: N999 - directory name is fixed (core/context-assembly/), matching
# the hyphenated convention used across this repo's top-level core/ modules
# (exec-assistant, model-router, etc.); renaming would require touching every
# sys.path.insert()/import site across parallel in-flight worktrees. Out of
# scope for this ruff-cleanup mission.
