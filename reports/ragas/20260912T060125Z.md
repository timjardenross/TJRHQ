# ragas RAG evaluation — 2026-09-12T06:01:25.006984+00:00

Mission: USS-TJR-MSN-0366 Stream 4. See
`knowledge/missions/RAGAS-RAG-EVAL-20260912-knowledge-record.md` for full context on
what "the RAG pipeline" turned out to be, and why the modes below may show fallback
paths.

| Setting | Mode |
|---|---|
| Retrieval | `fallback-repo-docs` |
| Generation | `fallback-fixture` |
| Judge | `offline-lexical-overlap` |

**Question:** Does retrieve_knowledge.py have a live caller today, and how does its keyword search actually work?

**Reference (ground truth):** retrieve_knowledge.py has no live caller — the OSS gap analysis (GAP 4) says it is never called and recommended wiring it to Meilisearch. Its keyword search now routes through Meilisearch as the primary path, falling back to the Supabase keyword_search_documents RPC and then to the ilike fallback_search.

**Retrieved contexts:**
- Keyword search now routes through Meilisearch (http://localhost:7700) as the primary path. If Meilisearch is unavailable or returns no hits, the call falls back automatically to the Supabase keyword_search_documents RPC and then to the ilike fallback_search — preserving full backward compatibility.
- GAP 4 — Search fragmented (6 incompatible implementations). 6 separate search implementations. retrieve_knowledge.py never called. Wave 4 consolidation mission not assigned. Recommendation: Meilisearch for search consolidation — single binary, hybrid search, replaces all 6 implementations with one client. Assign Wave 4 mission to wire retrieve_knowledge.py to Meilisearch.

## Scores

Context precision and context recall are retriever-quality metrics — they depend on
the question, retrieved contexts, and reference, not on which generated answer is
being judged, so they are computed once.

| Metric | Score |
|---|---|
| context_precision | 1.0000 |
| context_recall | 1.0000 |

Faithfulness depends on the generated answer, so it's computed once per case to show
the metric actually discriminating grounded vs. hallucinated output:

| Case | Answer | faithfulness |
|---|---|---|
| grounded | retrieve_knowledge.py is not called by anything live in production. Its keyword search primarily uses Meilisearch, falling back to the Supabase keyword_search_documents RPC and then an ilike-based fallback_search if that RPC fails. This matches GAP 4's recommendation to wire retrieve_knowledge.py to Meilisearch for search consolidation. | 1.0000 |
| hallucinated | retrieve_knowledge.py is not called by anything live in production. Its keyword search primarily uses Meilisearch, falling back to the Supabase keyword_search_documents RPC and then an ilike-based fallback_search if that RPC fails. This matches GAP 4's recommendation to wire retrieve_knowledge.py to Meilisearch for search consolidation. This retrieval pipeline has already been deployed as the live answer-generation path behind the Command Layer chat interface since August 2026. | 0.7500 |
